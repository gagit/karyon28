{{ "{% extends '"}}{{ bundle }}{{"::layout.html.twig' %}" }}
{{ "{% block contenido -%}" }}
<div class="panel-heading">
  <h4> 
  <span class="glyphicon glyphicon-plus-sign"></span>  Crear nuevo/a {{ document }}
 </h4>
</div>
<hr/>
<form action="{{ "{{ path('"~ route_name_prefix ~"_create') }}" }}" method="post" {{ "{{ form_enctype(form) }}" }}>
    {{ "{{ form_widget(form) }}" }}
    <p>
        <button type="submit" class="btn btn-default"> 
            <span class="glyphicon glyphicon-edit"></span>Crear</button>
    </p>
</form>

{% set hide_edit, hide_delete = true, true %}
{% include 'views/others/record_actions.html.twig' %}
{{ "{% endblock %}" }}

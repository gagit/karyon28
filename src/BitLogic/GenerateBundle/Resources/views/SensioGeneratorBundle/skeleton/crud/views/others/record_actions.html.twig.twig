<hr>
        <hr>
        <div class="btn-toolbar" role="toolbar" >
            <div class="btn-group" role="group" >
                <a href="{{ "{{ path('" ~ route_name_prefix ~ "') }}" }}"class="btn btn-default"
                    data-toggle="tooltip" data-placement="top"  title="Click para Volver al listado" data-animation="true" >
                    <i class="fa fa-arrow-left"></i>
                </a>
          </div>
{% if ('edit' in actions) and (not hide_edit) %}
    <div class="btn-group" role="group" >
        <a href="{{ "{{ path('" ~ route_name_prefix ~ "_edit', { 'id': entity.id }) }}" }}" class="btn btn-default" 
                      data-toggle="tooltip" data-placement="top"  title="Click para hacer cambios" data-animation="true" >
                 <i class="fa fa-edit" ></i> Editar
        </a>
    </div>
{% endif %}
{% if ('delete' in actions) and (not hide_delete) %}
    <div class="btn-group" role="group" >
        {{ '{{ form_start(delete_form) }}' }}
            {{ '{{ form_widget(delete_form.submit) }}' }}
        {{ '{{ form_end(delete_form) }}' }}
    </div>
{% endif %}
</div>
